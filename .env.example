# Copy to .env.local for local dev (gitignored by Vite's *.local pattern),
# or set in your hosting provider's env vars (Cloudflare Pages, etc.).

# Base URL of the worldview-api backend. No trailing slash.
# Local dev default if unset: http://127.0.0.1:8088
VITE_API_BASE=http://127.0.0.1:8088
